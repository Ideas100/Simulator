
/*
 * The pin number is based on the objects that were predefined.
 * For digital pins,
 *  - d0, d1, d2, d3, d4, d5, d6, d7, d8, d9, d10, d11, d12, d13.
 * For analog pins,
 *  - a0, a1, a2, a3, a4, a5.
 * Each function should have objects of the pin number that were decalred.
 *
 * For example:- 
 * Decalaring the pinMode:-
 *  - Arduino version : pinMode(pin_no, mode);
 *  - Our version     : obj.pinmode(pin_no, mode);
 *  i.e., if   - pinMode(10, HIGH);
 *        then - d10.pinMode(10, HIGH);
 *
 * For reference check test directory..!
 */

/*------------------------- Add your code below ----------------------------*/


void setup()
{
	
}


void loop()
{
	
}
